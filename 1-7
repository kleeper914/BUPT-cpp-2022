#include <iostream>
using namespace std;

int main()
{
	int n = 0;
	char ch = 0;

	cin >> n;
	if (n >= 32 && n <= 115)
	{
		ch = n;
		for (int i = 0; i < 9; i++)
		{
			cout << (char)(ch + i) << " ";
		}
		cout << (char)(ch + 9);
	}
	return 0;
}
