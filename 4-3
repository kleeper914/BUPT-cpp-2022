#include <iostream>
using namespace std;

int main()
{
	int num = 0;
	int max = 0;

	for(int i = 0; ; i++)
	{
		cin >> num;
		max = num;
		if (num == 0) break;
		while (num != 0)
		{
			cin >> num;
			if (num > max) max = num;
		}
		cout << max << endl;
	}
	return 0;
}
