#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
	double t = 0;
	int n = 0;

	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		t += 1 / (double)i;
	}
	cout << setiosflags(ios::fixed) << setprecision(6) << t;
	return 0;
}
